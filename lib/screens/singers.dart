import 'package:flutter/material.dart';

class SingersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mahlete Eyesus')),
      body: const Center(
        child: Text('Mezmur Application'),
      ),
    );
  }
}
